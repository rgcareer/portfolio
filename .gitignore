# build output
dist/
.astro/

# deps
node_modules/

# os / editor
.DS_Store
*.log

# env / secrets — never commit
.env
.env.*
!.env.example

# internal planning + local tooling — keep OUT of the public repo
tasks/
.claude/
