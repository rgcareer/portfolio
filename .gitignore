# build output
dist/
.astro/

# deps
node_modules/

# os / editor
.DS_Store
*.log

# local tooling scratch + stray capture output (never commit)
.playwright-mcp/
/proto*.png
/*-hero*.png

# env / secrets — never commit
.env
.env.*
!.env.example

# internal planning + local tooling — keep OUT of the public repo
tasks/
.claude/
